import { BigImageResizingDirective } from './big-image-resizing.directive';

describe('BigImageResizingDirective', () => {
  it('should create an instance', () => {
    const directive = new BigImageResizingDirective();
    expect(directive).toBeTruthy();
  });
});
